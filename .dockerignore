# .dockerignore

# Ignore node modules, as they will be installed inside the container
node_modules

# Ignore git history and local environment files
.git
.gitignore
.env

# Ignore Docker-related files to prevent them from being copied into the image
Dockerfile
docker-compose.yml
docker-entrypoint.sh